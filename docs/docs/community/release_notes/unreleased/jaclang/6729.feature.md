- **Feature: declare archetype uniqueness**: A node archetype can now be registered as unique, either globally or per owning root, so concurrent find-or-create flows can no longer produce duplicate nodes; a conflicting write raises a typed uniqueness error instead of silently creating a second node (#6266).
