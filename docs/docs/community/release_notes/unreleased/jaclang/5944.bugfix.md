- **Fix: add `acall_llm` async hook to `JacByLLM`**: Adds `acall_llm` to the `JacByLLM` hook interface (`runtime.jac`), registers it in the hook dispatch list (`jaclib.jac`), and provides a base stub implementation (`runtime.impl.jac`). Enables plugins (e.g. byllm) to supply a non-blocking async LLM call path for async walkers without changing the existing sync `call_llm` contract.
