- **Security + Perf: faster auth and instant token revocation**: Authenticated requests skip repeated cryptographic work via a process-local token cache (30 s TTL). Changing your password or deleting your account now immediately invalidates all active tokens; a stolen credential can no longer keep an active session alive. Each new token carries a unique `jti` ID, laying the groundwork for per-device logout in a future release.
