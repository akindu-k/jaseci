- **Cross-pod cache consistency**: In multi-pod deployments, a write on one pod now invalidates the affected node across sibling pods' in-memory caches, so concurrent edge-creating walkers no longer read stale data (which previously caused duplicate nodes such as duplicate user profiles).
