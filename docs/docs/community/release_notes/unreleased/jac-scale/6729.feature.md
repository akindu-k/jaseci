- **Feature: enforced unique nodes on MongoDB**: Declared archetype uniqueness is enforced by a MongoDB unique index, so racing requests that create the same node converge to one node; the losing request receives an HTTP 409 conflict instead of producing a duplicate (#6266).
