- **Empty dictionary fields now persist reliably on MongoDB.** Archetype `dict` fields that are empty, or cleared back to `{}`, are now saved correctly instead of being silently dropped after the first write. This previously failed on MongoDB versions older than 6.1.0, which reject empty embedded objects.
