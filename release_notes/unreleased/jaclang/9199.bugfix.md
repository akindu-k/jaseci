- **Fix: Deploys catch `jac.toml` values the pods cannot resolve**: `jac scale deploy`, including `--dry-run`, now stops before anything ships when the shipped `jac.toml` references an environment variable the pods will not have, and lists every such setting at once. Supply the variable through `[scale.secrets]` or the app's env, or give it a `${VAR:-default}`.
