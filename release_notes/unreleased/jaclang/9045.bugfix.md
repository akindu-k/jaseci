- **Fix: Unset `jac.toml` environment variables now fail loud**: `${VAR}` and `${VAR:?message}` values with no environment variable set now reject the configuration with a clear error instead of silently passing through as literal text.
